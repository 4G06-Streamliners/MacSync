# Private Registry Configuration (Verdaccio / @large-event removed; use default registry)

# React Native compatibility - hoist all dependencies to avoid multiple React copies
shamefully-hoist=true
node-linker=hoisted
